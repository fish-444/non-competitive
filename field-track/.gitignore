# ── 원본 실측·라이선스 데이터 (절대 커밋 금지) ─────────────────
# 지시문: "라이선스가 필요한 데이터나 원본 실측 파일 커밋" 금지.
# 한 번 올라가면 히스토리에서 지우기 어렵습니다. 확장자가 아니라 폴더로 막습니다.
data/raw/
data/licensed/
data/private/
*.xlsx
*.xls
*.hwp
*.hwpx

# 기상 원자료 (기상청 ASOS/AWS, SSP 시나리오 등) — 용량과 재배포 조건 문제
data/weather/
*.grib
*.grib2
*.nc

# 다만 아래는 추적합니다: 가공 결과·스키마·소량 샘플
!data/processed/
!data/schema/
!data/sample/

# ── DSSAT ──────────────────────────────────────────────────
# Fortran 실행파일과 파일 기반 입출력물. 용량·라이선스 문제로 커밋하지 않습니다.
dssat/
*.CUL
*.ECO
*.SPE
*.WTH
*.SOL
*.OUT

# ── 비밀 ───────────────────────────────────────────────────
.env
.env.*
!.env.example
*.key
*.pem

# ── 파이썬 ─────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# ── Node ───────────────────────────────────────────────────
node_modules/
.next/

# ── OS·에디터 ──────────────────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
